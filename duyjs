simply.setText({
  title: 'Victory',
  subtitle: '30/2/3'
}, true);

e = {
  button: 'select',
};

simply.on('singleClick', function(event) {
  if(e.button == 'select'){
  simply.setText({body: 'Heimerdinger'}];
  }
});
